# macOS Build (Homebrew + PyInstaller) – Skeleton

## 1) Voraussetzungen
- Homebrew
- `brew install python gtk4 pygobject3 adwaita-icon-theme`

## 2) Build
- `pip install -U pip pyinstaller`
- PyInstaller build in `build.sh` (Skeleton)

## 3) Codesigning/Notarization
Für echte Distribution später: codesign + notarize.
